{
  "name": "kingsukh-backend",
  "version": "1.0.0",
  "description": "Backend for Kingsukh Guest House",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && cd ../frontend && npm install && npm run build",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.22.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
